package cribbage;

public class RunsRule implements ScoreRule {
    //todo: implement runs scoring
    // consider using strategy pattern for this (for the diff runs?)

    public RunsRule() {}

    public int getScore() {
        int score = 0;
        // for each incrementing/types of runs score++
        return score;
    }
}
