package cribbage;

public class FlushRule implements ScoreRule {
    //todo: scoring - all four cards in the hand are of the same suit (flush4) earns 4 points
    // additional point if the starter card is also of that suit (flush5)

    public FlushRule() {}

    public int getScore() {
        int score = 0;
        //for each run score increase etc.
        return score;
    }
}
